int main()
{
    // this program shows in fact we can write own code without even using stdio.h library. but we can do so much without using libraries. 
    // Libraries are good and helping us to speed up development process!
    // this program compiles and runs. but doesnt do anything meaningfull because we dont even use standart io stream library1
    int a = 1, b = 2, c = 0;
    c = a + b;
}